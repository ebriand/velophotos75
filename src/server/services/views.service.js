export const incPhotosetView = (photosetId) => {
  console.log(`Inc number of views of photoset id ${photosetId}`)
}

export const incPhotoView = (photoId) => {
  console.log(`Inc number of views of photo id ${photoId}`)
}
